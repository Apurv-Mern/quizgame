/**
 * Models Index
 * Exports domain models for the application
 */

const Participant = require('./Participant');
const Question = require('./Question');

module.exports = {
    Participant,
    Question
};
